# Store Manager Components

مكونات مدير المتجر التابع - تم تقسيمها من ملف واحد كبير (1,303 سطر) إلى مكونات صغيرة قابلة للصيانة.

## 📁 هيكل المجلد

```
store-manager/
├── types.ts                      # أنواع البيانات المشتركة
├── useStoreManager.ts            # Hook لإدارة حالة المتجر الأساسية
├── useCategoriesManagement.ts    # Hook لإدارة الفئات
├── useHeroSettings.ts            # Hook لإدارة القسم الرئيسي
├── StoreHeader.tsx               # رأس المتجر
├── GeneralSettingsTab.tsx        # تبويب الإعدادات العامة
├── AppearanceTab.tsx             # تبويب المظهر
├── HeroSectionTab.tsx            # تبويب القسم الرئيسي
├── CategoriesTab.tsx             # تبويب الفئات
├── SharingTab.tsx                # تبويب المشاركة
├── AnalyticsTab.tsx              # تبويب الإحصائيات
├── TabsNavigation.tsx            # التنقل بين التبويبات
├── index.ts                      # Barrel export
└── README.md                     # هذا الملف
```

## 🔧 المكونات

### 1. **StoreHeader** (رأس المتجر)
- عرض معلومات المتجر الأساسية
- أزرار المعاينة والتعديل
- عرض الشعار والإحصائيات

### 2. **GeneralSettingsTab** (الإعدادات العامة)
- تعديل اسم المتجر
- تعديل وصف المتجر
- نسخ رابط المتجر

### 3. **AppearanceTab** (المظهر)
- اختيار الثيم
- رفع الشعار
- إعدادات إضافية

### 4. **HeroSectionTab** (القسم الرئيسي)
- رفع صورة الخلفية
- تعديل العنوان والوصف
- إعدادات زر الإجراء (CTA)

### 5. **CategoriesTab** (الفئات)
- اختيار طريقة عرض الفئات
- إضافة/تعديل/حذف الفئات
- تفعيل/تعطيل الفئات

### 6. **SharingTab** (المشاركة)
- رابط المتجر
- توليد رمز QR
- المشاركة على وسائل التواصل

### 7. **AnalyticsTab** (الإحصائيات)
- المشاهدات والنقرات
- معدل التحويل
- إجمالي المبيعات

### 8. **TabsNavigation** (التنقل)
- قائمة منسدلة للجوال
- تبويبات للشاشات الكبيرة

## 🎣 Custom Hooks

### 1. **useStoreManager**
إدارة الحالة الأساسية للمتجر:
- حالة التعديل (isEditing)
- بيانات التعديل (editData)
- رابط المتجر (storeUrl)
- وظائف الحفظ والنسخ والمشاركة

### 2. **useCategoriesManagement**
إدارة الفئات:
- تحميل الفئات المتاحة
- تحميل منتجات المتجر
- إضافة/تعديل/حذف الفئات
- تفعيل/تعطيل الفئات
- حفظ إعدادات العرض

### 3. **useHeroSettings**
إدارة القسم الرئيسي:
- حالة إعدادات Hero
- رفع الصور
- حفظ الإعدادات

## 📦 الاستخدام

### استيراد من الملف الرئيسي

```typescript
import {
  // Components
  StoreHeader,
  GeneralSettingsTab,
  AppearanceTab,
  HeroSectionTab,
  CategoriesTab,
  SharingTab,
  AnalyticsTab,
  TabsNavigation,

  // Hooks
  useStoreManager,
  useCategoriesManagement,
  useHeroSettings,

  // Types
  type AffiliateStoreManagerProps,
  type CurrentSection,
  type TabValue
} from './store-manager';
```

### مثال على الاستخدام

```typescript
export const AffiliateStoreManager = ({ store, onUpdateStore }: AffiliateStoreManagerProps) => {
  // استخدام الـ hooks
  const { isEditing, editData, storeUrl, handleSaveChanges } = useStoreManager(store, onUpdateStore);
  const { heroSettings, saveHeroSettings } = useHeroSettings(settings, updateSettings, refetch, uploadImage);

  return (
    <div>
      <StoreHeader store={store} storeUrl={storeUrl} isEditing={isEditing} />
      <Tabs>
        <TabsNavigation activeTab={activeTab} onTabChange={handleTabChange} />
        <TabsContent value="general">
          <GeneralSettingsTab {...props} />
        </TabsContent>
        {/* ... باقي التبويبات */}
      </Tabs>
    </div>
  );
};
```

## ✨ المزايا

### قبل التقسيم:
- ❌ ملف واحد كبير (1,303 سطر)
- ❌ صعوبة الصيانة
- ❌ صعوبة اختبار الأجزاء المنفصلة
- ❌ إعادة استخدام محدودة

### بعد التقسيم:
- ✅ ملف رئيسي صغير (~250 سطر)
- ✅ مكونات منفصلة قابلة للصيانة
- ✅ سهولة الاختبار
- ✅ إمكانية إعادة الاستخدام
- ✅ Custom hooks منظمة
- ✅ TypeScript types واضحة
- ✅ استخدام semantic tokens
- ✅ تعليقات بالعربية

## 🎯 الإحصائيات

| المقياس | قبل | بعد |
|--------|-----|-----|
| عدد الأسطر (الملف الرئيسي) | 1,303 | ~250 |
| عدد الملفات | 1 | 13 |
| عدد المكونات | 0 | 8 |
| عدد الـ Hooks | 0 | 3 |
| قابلية الصيانة | منخفضة | عالية |

## 📝 ملاحظات

- جميع المكونات تستخدم **semantic tokens** فقط
- جميع الـ exports هي **named exports**
- جميع التعليقات بالعربية
- لم يتم تغيير أي **business logic**
- تم الحفاظ على جميع الوظائف الأصلية

## 🔄 الصيانة المستقبلية

عند إضافة تبويب جديد:
1. أنشئ component جديد في المجلد
2. أضف الـ props types
3. استخدم semantic tokens
4. صدّر من `index.ts`
5. استخدم في الملف الرئيسي

---

**تم التطوير بواسطة:** Claude (Anthropic)
**التاريخ:** 2025-11-23
**الإصدار:** 1.0.0
